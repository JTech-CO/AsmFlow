# Packaging

This directory contains release-install templates. The current package is a specification scaffold and does
not include binaries. Before enabling hardening options in a release, test them against configured MCP child
processes and document any required exceptions. Release packaging must include License, Notice, Schema,
examples, man pages, SBOM, and checksums.
