# Copy to a private environment file outside version control and chmod 600.
# These are placeholders, not working credentials.
OPENAI_API_KEY=replace-me
ASMFLOW_GATEWAY_TOKEN=replace-me
FILESYSTEM_TOKEN=replace-me
REMOTE_MCP_TOKEN=replace-me
